Shader {
  Rectangle rec = (50, 50, Color.black);
  rec.position(110, 110 + cos(Time) * 100);

  Rectangle rec2 = (50, 50, Color.yellow);
  rec2.position(110 + sin(Time) * 100, 110);

  Scene.color = Color.red;
  Scene.add(rec);
  Scene.add(rec2);
}