Shader {
  Rectangle rec = (50, 50, Color.Black);
  rec.setPosition(150, 150 + cos(Time) * 100);

  Rectangle rec2 = (50, 50, Color.Green);
  rec2.setPosition(150 + sin(Time) * 100, 150);

  Scene.setColor = Color.Red;
  Scene.add(rec);
  Scene.add(rec2);
}