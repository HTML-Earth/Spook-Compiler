Shader {
    CustomShape custom;

    Circle ball = (GiveMeTen(0), Color.red());
    ball.setPosition(xPos, yPos);
    Scene.add(ball);

    Scene.setColor(Color.black());
}


Class CustomShape {
    Num sides = 4;
}

Float GiveMeTen(Num irrelevant) {
    return 10;
}