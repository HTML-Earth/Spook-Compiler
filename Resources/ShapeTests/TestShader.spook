Shader {
    CustomShape custom = (1);

    Circle ball = (GiveMeTen(0), Color.red());
    ball.setPosition(xPos, yPos);
    Scene.add(ball);

    Scene.setColor(Color.black());
}


Class CustomShape {
    Num i;

    Num sides = 4;
}

Num GiveMeTen(Num irrelevant) {
    return 10;
}