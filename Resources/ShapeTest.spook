Shader {
  Rectangle rec = (50, 50, Color.Black);
  rec.position(0, 50);

  Rectangle rec2 = (50, 50, Color.Black);
  rec2.position(50, 0);

  Scene.color = Color.Red;
  Scene.add(rec);
  Scene.add(rec2);
}