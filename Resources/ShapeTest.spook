Shader {
  Rectangle rec = (50, 50, Color.black());

  Vec2 swizzlePosizzle = (25, 75);

  Num xPos = swizzlePosizzle.x;
  Num yPos = swizzlePosizzle.y;

  rec.setPosition(xPos, yPos);

  Rectangle rec2 = (50, 50, Color.yellow());
  rec2.setPosition(75, 25);

  Circle circle = (25, Color.blue());
  circle.setPosition(75, 75);

  Scene.setColor(Color.red());
  Scene.add(rec);
  Scene.add(rec2);
  Scene.add(circle);
}